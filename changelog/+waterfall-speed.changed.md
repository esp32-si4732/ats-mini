Sped up the RSSI waterfall refresh. The per-scan point count (`WATERFALL_POINTS`) was reduced from 100 to 60 so each scan row completes faster (the on-device and web renderers already interpolate the points across the full width), and the web client now polls the running scan every 120ms (was 500ms) and re-arms the next scan after 60ms (was 300ms), tightening the loop between rows. The per-point SI473x tuning delay (`WATERFALL_TUNE_DELAY`) is left at 10ms, already at the safe minimum for valid RSSI reads.
